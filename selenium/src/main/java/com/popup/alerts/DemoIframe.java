package com.popup.alerts;
/*
	Basically, we can switch over the elements in frames using 3 ways.

	1.By Index
	2.By Name or Id
	3.By Web Element
	
	1. Suppose if there are 100 frames in page, we can switch to the iframe by using index.

       driver.switchTo().frame(0);
       driver.switchTo().frame(1);
       
    2.Name and ID are attributes of iframe through which we can switch to the it.

       driver.switchTo().frame("iframe1");
       driver.switchTo().frame("id of the element");   
	
	
	// we should iframe by Right Clicking on the page by view page source
	// if image Right Click it will show ((""This Frame""))
	
	
	
	
*/
